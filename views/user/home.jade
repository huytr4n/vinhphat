h2 users
- for (var i = 0; i < users.length; i++)
	span #{i+1}  
	span #{users[i].name} 
	span #{users[i].username} 
	a(href='/user/del/#{users[i]._id}') remove
	br

br
br
a(href='/user/add') add new user